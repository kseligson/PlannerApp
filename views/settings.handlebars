<!doctype html>

<html>
<head>
	<title>People's Planner</title>	
	<meta charset="utf-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<link href="/css/bootstrap.min.css" rel="stylesheet">
	<link href="/css/bootstrap-theme.min.css" rel="stylesheet">
	<link href="/css/project.css" rel="stylesheet">
	<!-- this is a comment in HTML -->

    <!-- HTML5 Shim and Respond.js IE8 support of HTML5 elements and media queries -->
    <!-- WARNING: Respond.js doesn't work if you view the page via file:// -->
    <!--[if lt IE 9]>
      <script src="https://oss.maxcdn.com/libs/html5shiv/3.7.0/html5shiv.js"></script>
      <script src="https://oss.maxcdn.com/libs/respond.js/1.3.0/respond.min.js"></script>
    <![endif]-->        
</head>

<body>
	{{> navbar}}

	<div class="container">
		<div class="settings-profile">
			<figure align="center">
				<img class="img-circle" id="profile-picture" src="img/default.png"/>
			</figure>
		</div>

		<div class="settings">
			<ul>
				<li><button id="notif-toggle">Notifications	<img id="toggle-arrow" style="width:10px; height: auto;"src="img/collapse-arrow.png"/></button></li>
				<li class="toggle-li" id="emailnotif">Email On</li>
				<li class="toggle-li" id="textnotif">Texting On</li>
				<li class="liref">
					<a href="/accountSettings">Account Settings</a>
				</li>
				<li class="liref">
					<a data-toggle="modal" data-target="#helpModal" href="#">Help</a>
				</li>
				<li class="liref">
					<a id="logout" href="#" onclick="window.location.reload">Logout</a>
				</li>
			</ul>
		</div>
	</div>

	{{> helpModal}}

	<script src="https://code.jquery.com/jquery.js"></script>
	<script src="/js/bootstrap.min.js"></script>


	<script src="https://ajax.googleapis.com/ajax/libs/jquery/1.12.0/jquery.min.js"></script>
	{{> facebook}}
    <script src="/js/facebook.js"></script>
		<script>
		$(document).ready(function(){
			//Toggle notification menu
		    $('.toggle-li').hide();
		    var arrow = "img/collapse-arrow.png";
		    $('#toggle-arrow').attr('src', arrow);
		    $("#notif-toggle").click(function(){
		        $('.toggle-li').slideToggle("fast");
		        if(arrow == "img/collapse-arrow.png") {
		        	arrow = "img/display-arrow.png";
				}
				else {
					arrow = "img/collapse-arrow.png";
				}
				$('#toggle-arrow').attr('src', arrow);
		    });
		    
		    //Toggle email on/off text
		    $("#emailnotif").click(function(){
		    	 $(this).text(function(i, text){
	          		return text === "Email On" ? "Email Off" : "Email On";
	      		})
		    });

		    //Toggle text on/off text
		    $("#textnotif").click(function(){
		    	 $(this).text(function(i, text){
	          		return text === "Texting On" ? "Texting Off" : "Texting On";
	      		})
		    });
		    //Logout of app
		    $('#logout').click('click', function(response) {
		      console.log('User is logging out of Facebook');
		      FB.logout(function(response) {
		        // user is now logged out
		        window.location = "/";
		      });
		    })
		});
	</script>
    {{> googleAnalytics}}
</body>
</html>
